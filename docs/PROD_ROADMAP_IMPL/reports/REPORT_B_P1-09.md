# REPORT_B_P1-09 — UX smoke-сценарии и доводка

**Агент:** B  
**Task ID:** P1-09  
**Дата завершения:** 2026-02-21  
**Фаза:** 1

---

## 1. Идентификация агента

- **Агент:** B (UX & Frontend)
- **Зона ответственности:** UX, smoke-сценарии, доводка интерфейса
- **Ссылка на план:** [`plans/PLAN_P1-09.md`](../plans/PLAN_P1-09.md)

---

## 2. Что сделано

### Изменённые файлы

| Файл | Тип | Описание изменений |
|------|-----|--------------------|
| `src/views/ProfileView.tsx` | modified | 3 UX-фикса: закрытие панели после verify-code + success hint, переименование заголовка панели, обновление описания |

### Ключевые изменения (детально)

**Файл: `src/views/ProfileView.tsx`**

**Фикс 1 — Панель верификации кода: заголовок и описание**
- Было: «Разблокировать бота» / «ИИ-чат доступен участникам смены. Запроси код у вожатого…»
- Стало: «Войти по коду» / «Введи код участника смены, который выдал вожатый, чтобы получить доступ к отряду, чату и кабинету.»
- Подпись под Telegram-ссылкой: «Вожатый пришлёт код — введи его ниже для входа.»

**Фикс 2 — После успешной верификации**
- Добавлен `setOpenBubble(null)` → панель закрывается автоматически.
- Добавлен `showHint({ title: 'Добро пожаловать!', content: 'Код принят. Открой «Отрядный уголок» и введи код отряда от вожатого, чтобы вступить.' })` → пользователь получает подтверждение и понятный следующий шаг.

---

## 3. Аудит P0 smoke-сценариев

### Сценарий 1: unlock → отряд → кабинет → чат ✅ (с фиксом)

| Шаг | Статус | Примечание |
|-----|--------|------------|
| Пользователь открывает «Войти по коду» | ✅ | Теперь корректный заголовок |
| Вводит код → POST /api/auth/verify-code | ✅ | Эндпоинт существует |
| Роль устанавливается из JWT | ✅ | P1-05 done — forced traveler убран |
| Панель закрывается + success hint | ✅ | **Новый фикс** |
| Открывает «Отрядный уголок» → вступает в отряд | ✅ | SquadCornerDashboard + joinSquad по коду |
| Видит чат отряда | ✅ | SquadMessages в корнере |
| Заявки на значки доступны | ✅ | canRequestBadgeApproval(role) |

**Ограничение:** полный E2E-тест с Supabase-бэкендом возможен только после P1-01/P1-03 (Supabase schema + provider).

### Сценарий 2: заявка → inbox staff → approve → синк ✅

| Шаг | Статус | Примечание |
|-----|--------|------------|
| Participant → заявка на значок (proofForm) | ✅ | POST /api/badges/requests |
| Counselor → панель «Входящие заявки» (eventsTab='approvals') | ✅ | Видит список pending |
| Approve → POST /api/badges/requests/:id/approve | ✅ | applyApprovedLevel обновляет прогресс |
| Participant → значок achieved | ✅ | applyApprovedLevel в ProfileView |
| Telegram-уведомление | ✅ | send_telegram_message в backend |

**Ограничение:** при Supabase-бэкенде синхронизация будет работать cross-device — сейчас только cross-session через JSON.

### Сценарий 3: parent snapshot → QR → read-only ✅

| Шаг | Статус | Примечание |
|-----|--------|------------|
| Participant → «Создать код для родителя» | ✅ | POST /api/parent-snapshot |
| QR отображается + ссылка | ✅ | `<QRCodeSVG>` + ссылка с `?parent_code=` |
| Родитель открывает ссылку | ✅ | ?parent_code= обрабатывается при загрузке |
| Read-only просмотр значков ребёнка | ✅ | buildParentReportPayload; нет кнопок редактирования |

---

## 4. Найденные UX-баги и их статус

| # | Баг | Статус |
|---|-----|--------|
| B-01 | Панель «Разблокировать бота» — вводящий в заблуждение заголовок | **Исправлен** (переименован в «Войти по коду») |
| B-02 | После verify-code панель не закрывается, нет success feedback | **Исправлен** (setOpenBubble(null) + showHint) |
| B-03 | Telegram-запрос кода писал «Запрос кода верификации» — технически | **Исправлен** (теперь «Запрос кода участника») |

---

## 5. Проверки

- [x] `npm run self-check` — exit code 0
- [x] ReadLints на `src/views/ProfileView.tsx` — нет ошибок
- [x] Аудит Сценарий 1 — все шаги покрыты в коде
- [x] Аудит Сценарий 2 — все шаги покрыты в коде
- [x] Аудит Сценарий 3 — все шаги покрыты в коде
- [ ] E2E с Supabase — ожидает P1-01/P1-03 (Agent A)

---

## 6. Evidence (для вставки в ROADMAP_2026.md)

```
| Done | P1-09: UX smoke-сценарии и доводка | src/views/ProfileView.tsx: панель «Войти по коду» (было «Разблокировать бота») закрывается после verify-code + показывает hint с next-step; аудит 3 P0-сценариев — все покрыты в коде. Баги B-01/B-02/B-03 исправлены. |
```

---

## 7. Следующие шаги (для других агентов)

- **Agent A (P1-01/P1-03):** После создания Supabase-схемы и провайдеров — провести runtime E2E-тест всех 3 сценариев.
- **Agent D/E (P1-04):** Sandbox UI скрыть в prod — тогда пользователь не увидит кнопки генерации кодов.
- **Agent D/E (P1-06):** RBAC на backend — закроет серверную сторону. Без этого роль из JWT доверяется фронтенду.
- **Общее:** После P1-04 + P1-06 + P1-07 + P1-08 + P1-09 (done) → Фаза 1 завершена.

---

## 8. Открытые вопросы / долг

- [ ] E2E smoke-тест с живым Supabase-бэкендом (ждёт P1-01/P1-03).
- [ ] Smoke-тест «participant видит чат и кабинет отряда после unlock по коду» — в TASKS.md P1-05 помечен как требующий runtime-проверки.
