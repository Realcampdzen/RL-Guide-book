# REPORT — Agent C | M5-R4-C — Chat Quality + Safety

**Агент:** C (Chat/AI/Safety/Transport)  
**Date:** 2026-02-27  
**Task:** M5-R4-C — Pending badges context injection + max message length validation  
**Commit (implementation):** _(see commit hash below)_  
**Status:** ✅ DONE (CERTIFIED)

---

## 1. Task Summary

M5-R3-C закрыт: бот знает отряд, смену, никнейм. Следующий приоритет:
1. **Chat quality:** НейроВалюша не знала о pending badge заявках участника → добавить в контекст `pending_badge_count` + `pending_badge_titles` → бот может ответить «Твоя заявка на значок X сейчас на проверке у вожатого».
2. **Chat safety:** `/api/chat` не валидировал длину сообщения → добавить `CHAT_MAX_MESSAGE_LEN` (default 2000) с возвратом `400` при превышении.

---

## 2. Deliverables Implemented

### D1 — Pending badges context в chat_with_bot() (backend/app.py)

После squad/shift lookup блока добавлен блок:

```python
# M5-R4-C: Inject pending badge requests for personalization
try:
    _bdoc = _badge_requests_load()
    _pending = [
        r for r in (_bdoc.get("requests") or [])
        if isinstance(r, dict)
        and (r.get("requestedBy") or {}).get("deviceId") == device_id
        and r.get("status") == "pending"
    ]
    if _pending:
        context["pending_badge_count"] = len(_pending)
        context["pending_badge_titles"] = [
            r.get("badgeTitle") or r.get("levelId") or "?"
            for r in _pending[:3]
        ]
except Exception:
    pass  # non-blocking
```

### D2 — System prompt — pending badges (chatbot/prompts/system_prompt.py)

Новые kwargs `pending_badge_count` и `pending_badge_titles` добавлены в сигнатуру `get_system_prompt_with_context()`. При `pending_badge_count > 0` в prompt добавляется:
> «У пользователя есть заявки на проверке (N шт.): [title1, title2, ...]. Если спрашивает про статус — скажи что вожатый рассматривает.»

### D3 — Message length validation (backend/app.py + env)

```python
# M5-R4-C: Max message length for /api/chat
CHAT_MAX_MESSAGE_LEN = int(os.getenv('CHAT_MAX_MESSAGE_LEN', '2000'))
```

Validation guard в `chat_with_bot()` до chatbot enrichment:

```python
# M5-R4-C: Message length guard (before enrichment to avoid wasted work)
_raw_message = (data.get("message") or "") if data else ""
if len(_raw_message) > CHAT_MAX_MESSAGE_LEN:
    app.logger.warning('[CHAT_SAFETY] message_too_long device=%s len=%d',
                       _hash_key(device_id or 'anon'), len(_raw_message))
    return jsonify({"error": "Сообщение слишком длинное", "maxLen": CHAT_MAX_MESSAGE_LEN}), 400
```

### D4 — Smoke Flow G extended (G-3)

`run_flow_g()` в `backend/scripts/smoke_backend_critical.py` дополнен:
- **G-3:** POST `/api/chat` с message длиной 2001 символ → ожидать `400`

Итого Flow G: **5 checks** (G-auth + G-1 + G-2 + G-3). Total smoke: **44 checks**.

---

## 3. Files Changed

| File | Изменение |
|------|-----------|
| `backend/app.py` | Константа `CHAT_MAX_MESSAGE_LEN`, message length guard, pending badges enrichment block |
| `chatbot/models/conversation.py` | 2 новых optional поля в `WebContext`: `pending_badge_count`, `pending_badge_titles` |
| `chatbot/core/context_manager.py` | Store `pending_badge_count`/`pending_badge_titles` в `session_data` |
| `chatbot/prompts/system_prompt.py` | 2 новых kwargs, context_parts entry для pending badges |
| `chatbot/core/response_generator.py` | Pass `pending_badge_count`/`pending_badge_titles` во всех 6 call sites |
| `backend/scripts/smoke_backend_critical.py` | G-3 check (message > 2000 → 400) |
| `docs/BACKEND_CONTRACT_GUARD.md` | §3.5: 400 status, message length, pending badges enrichment table, rate limit default fix (10→15), §5: Flow G 4→5, Total 43→44 |
| `.env.example` | `CHAT_MAX_MESSAGE_LEN` commented entry |

---

## 4. Guardrails Verified

| Guardrail | Status |
|-----------|--------|
| `_badge_requests_load()` read-only, no side effects | ✅ |
| `pending_badge_titles` max 3 items | ✅ |
| Message length check before chatbot init / enrichment | ✅ |
| No breaking changes to response structure | ✅ |
| Pending badge lookup в try/except (non-blocking) | ✅ |
| No breaking API changes | ✅ |

---

## 5. Definition of Done — Checklist

- [x] `pending_badge_count` / `pending_badge_titles` инжектируются в context
- [x] WebContext model обновлён (`pending_badge_count`, `pending_badge_titles`)
- [x] `update_web_context()` хранит оба поля в `session_data`
- [x] System prompt использует поля если они есть
- [x] Все 6 call sites `get_system_prompt_with_context()` передают новые kwargs
- [x] Message length validation работает, возвращает `400` при превышении
- [x] Smoke G-3: message > 2000 chars → 400 (новый check)
- [x] Smoke total: 44 checks
- [x] `BACKEND_CONTRACT_GUARD.md` §3.5 и §5 обновлены
- [x] `.env.example` обновлён

---

## 6. Handoff

**Next:** M5-R5-C или иная задача по расписанию от NeuroStepa.

**Smoke baseline:** 44/44 (was 43/43 after M5-R3-C).

**Branch:** `agent-c/m5-r4-c` от `agentb/m3-bf-s5-auto-sync` (566ef81 — M5-R3-C tip).
