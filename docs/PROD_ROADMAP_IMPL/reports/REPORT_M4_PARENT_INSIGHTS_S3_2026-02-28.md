# REPORT — M4-PARENT-INSIGHTS-S3

## Delivered
1. Explainability payload in Parent Insights read-model
2. Parent UI block "Почему такая рекомендация"
3. Tone polish for parent-friendly, supportive wording

## Backend
File: `backend/app.py`

### Additive fields (non-breaking)
`GET /api/parent-insights?code=...` now returns optional:
- `whyThisSuggestion` (short stable explanation)
- `basedOn` (compact safe factors):
  - `trend`
  - `strongestAreas` (titles)
  - `weakestAreas` (titles)
  - `activityWindow`

No existing fields were removed or changed.

### Explainability behavior
- `whyThisSuggestion` is generated by deterministic rules from trend + strengths/weaknesses.
- Text remains short and stable for small fluctuations.
- No PII in `basedOn`.

## Frontend
File: `src/views/ProfileView.tsx`
- Added compact explainability section near parent nextSteps:
  - short "почему" line
  - compact "основа" line (trend + activity window)
- No new CTA, no layout redesign.

## Docs
- Updated: `docs/PARENT_INSIGHTS_READ_MODEL.md`
  - explainability fields
  - tone principles

## Smoke summary
- participant: no regressions ✅
- parent: explainability visible, recommendations readable, read-only preserved ✅
- staff: unaffected ✅

## Validation
- `python -m py_compile backend/app.py` ✅
- `npm run build` ✅

## Guardrails
- M2 read-only guard: untouched
- RBAC changes: none
- DB migrations: none
- write-flow changes: none
