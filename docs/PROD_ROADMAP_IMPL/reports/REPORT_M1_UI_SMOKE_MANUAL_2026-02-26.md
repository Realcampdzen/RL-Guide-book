# REPORT_M1_UI_SMOKE_MANUAL_2026-02-26

## Ручной UI smoke (staging/local)

Проведён быстрый ручной проход формы создания Движка.

## Подтверждено

1. Форма создания Движка доступна и рабочая (Движок успешно создан).
2. Selector scope (`camp/shift/squad`) отображается в UI.
3. Для `squad` отображаются оба поля контекста:
   - `shiftId`
   - `squadId`

(подтверждено скриншотом из теста)

## Статус

- Минимальный UI smoke для scoped-функционала подтверждён.
- Рекомендуется добить расширенный сценарий (конфликт-ветки + фильтрация через query) отдельным коротким проходом, но блокеров для M1 scoped shape не выявлено.
