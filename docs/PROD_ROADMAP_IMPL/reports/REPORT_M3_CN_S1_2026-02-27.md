# REPORT — M3-CN-S1 (Council Deepening — Slice 1)

## Delivered
1. Unified initiative status lifecycle for UI read-model:
   - `new -> reviewing -> accepted|rejected|done`
2. Added compact status-chip in Council initiatives list/cards.
3. Added basic filters by status; sorting by freshness preserved (newest first).
4. Backward compatibility preserved for current initiative creation and role flows.

## Backend
File: `backend/app.py`
- Added legacy status mapper `_map_council_status_legacy(...)`.
- `GET /api/council/initiatives` now adds non-breaking read-model fields:
  - `readStatus`
  - normalized `createdAt`
- Existing payload fields kept unchanged.

## Frontend
Files:
- `src/components/CouncilDashboard.tsx`
- `src/styles/profile-view.css`

Changes:
- Loaded initiatives in `camp-management` tab.
- Added status chip per initiative card.
- Added additive filter controls (`all/new/reviewing/accepted/rejected/done`).
- Default behavior unchanged: list sorted by freshness.

## Docs
- `docs/COUNCIL_STATUS_MODEL.md` (legacy mapper contract + read-model notes)

## Regression summary
- participant: initiative list + status chips + filters work ✅
- parent read-only (M2): not touched; no regression observed ✅
- staff: initiative view and existing flows intact ✅

## Validation
- `python -m py_compile backend/app.py` ✅
- `npm run build` ✅

## Out of scope check
- RBAC changes: none
- DB migrations: none
- big Council redesign: none
- moderation v2 complexity: none
