# REPORT — Agent C | M5-R3-C — Chat Context Enrichment

**Date:** 2026-02-27  
**Agent:** C (Chat/AI/Safety)  
**Task:** M5-R3-C — Enrich chat context with squad/shift/nickname from JWT + membership  
**Commit (implementation):** `248e456`  
**Status:** ✅ DONE (CERTIFIED)

---

## 1. Task Summary

НейроВалюша знала только `user_role` из JWT. Задача: добавить в системный промпт `nickname` участника, `squad_name` и `shift_name` — чтобы бот мог давать персонализированные ответы типа «Привет, Алекс! Ты в отряде "Дельфины" смены "Реальный Лагерь 2026"».

---

## 2. Implementation

### D1 — Enrichment in `chat_with_bot()` (`backend/app.py`)

После строки `context["user_role"] = payload.get("role")` добавлен блок:

```python
# M5-R3-C: Enrich context with membership/identity data from JWT
context["nickname"] = (payload.get("nickname") or "").strip() or None

if device_id and not context.get("squad_name"):
    try:
        _camp, _squad = _resolve_membership_context(device_id)
        if _squad:
            _shifts_doc = _shifts_load()
            _squad_obj  = _find_squad(_shifts_doc, _squad) or {}
            _sq_name = (_squad_obj.get("name") or "").strip() or None
            context["squad_name"] = _sq_name
            _shift_id = (_squad_obj.get("shiftId") or "").strip()
            if _shift_id:
                for _sh in (_shifts_doc.get("shifts") or []):
                    if isinstance(_sh, dict) and _sh.get("id") == _shift_id:
                        context["shift_name"] = (_sh.get("name") or "").strip() or None
                        break
    except Exception:
        pass  # non-blocking: lookup failure must never block chat response
```

**Guardrails соблюдены:**
- Lookup только при `device_id` присутствует и `squad` найден в membership
- `try/except` вокруг всего блока — любая ошибка файловой системы или данных не блокирует ответ
- `_squads_load()` не существует в кодовой базе — использованы существующие `_shifts_load()` + `_find_squad()` корректно

### D2 — `WebContext` pydantic model (`chatbot/models/conversation.py`)

Добавлены 3 новых опциональных поля:

```python
nickname: Optional[str] = Field(None, description="Никнейм участника из JWT")
squad_name: Optional[str] = Field(None, description="Название отряда (из membership lookup)")
shift_name: Optional[str] = Field(None, description="Название смены (из membership lookup)")
```

### D3 — `update_web_context()` (`chatbot/core/context_manager.py`)

После `context.session_data['user_role']`:

```python
context.session_data['nickname']    = getattr(web_context, 'nickname',    None)
context.session_data['squad_name']  = getattr(web_context, 'squad_name',  None)
context.session_data['shift_name']  = getattr(web_context, 'shift_name',  None)
```

### D4 — System prompt (`chatbot/prompts/system_prompt.py`)

Добавлены 3 kwargs в `get_system_prompt_with_context()`:

```python
nickname: Optional[str] = None,
squad_name: Optional[str] = None,
shift_name: Optional[str] = None,
```

В `context_parts` (после `user_role`):

```python
if nickname:
    context_parts.append(f"Никнейм пользователя: {nickname}")
if squad_name:
    context_parts.append(f"Отряд пользователя: {squad_name}")
if shift_name:
    context_parts.append(f"Смена: {shift_name}")
```

Персонализирующая инструкция (аддитивно):

```python
if nickname or squad_name:
    context_section += "\nОбращайся к пользователю по нику если он указан. Упоминай название отряда когда это уместно."
```

### D5 — Response generator (`chatbot/core/response_generator.py`)

Все 7 вызовов `get_system_prompt_with_context()` обновлены — добавлены kwargs:

```python
nickname=context.session_data.get('nickname'),
squad_name=context.session_data.get('squad_name'),
shift_name=context.session_data.get('shift_name'),
```

Проверка через AST: все вызовы содержат все 3 новых kwarg (`ALL calls have nickname/squad_name/shift_name`).

### D6 — Smoke Flow G (`backend/scripts/smoke_backend_critical.py`)

Добавлен `run_flow_g()`:

- **G-auth**: `auth/verify-code (participant)` → accessToken
- **G-1**: `POST /api/chat` + valid JWT + `{"message": "Как меня зовут?", "user_id": device}` → expect 200 + `response` field present
- **G-2**: `POST /api/chat` + `Bearer invalid.token.here` → expect 401

Подключён в `run()` после `run_flow_f()`. Docstring и WARNING обновлены.

### D7 — `docs/BACKEND_CONTRACT_GUARD.md`

- §1 Scope: добавлена строка `Chat | POST /api/chat`
- §3.5 новая секция `/api/chat`: auth, request body (mandatory + optional + enriched server-side), response mandatory fields, HTTP statuses, rate limits, breaking/non-breaking
- §5 Smoke table: добавлен Flow G (4 checks), Total обновлён 39 → 43
- Flow G описание добавлено под таблицей
- Last updated обновлён

---

## 3. Smoke Test Output

```
Smoke backend critical flows — http://localhost:4000
============================================================
[Health]
  PASS  /api/health status

[Flow A] Badge Request: request -> inbox -> approve -> mine
  SKIP  (no AUTH_SECRET — auth flows require it)

... (Flows B-G SKIP без AUTH_SECRET)

============================================================
RESULT: ALL 1 CHECKS PASSED
```

*(Без AUTH_SECRET только health; flows A-G SKIP по ожидаемой логике. Синтаксис: py_compile — 0 ошибок на всех 6 файлах.)*

---

## 4. Guardrails Check

| Guardrail | Status |
|-----------|--------|
| Lookup только при наличии membership/squad | ✅ — `if _squad:` guard |
| try/except вокруг lookup | ✅ — весь блок обёрнут |
| Не изменена структура ответа /api/chat | ✅ — response-поля не тронуты |
| Системный промпт — только аддитивное дополнение | ✅ — `context_parts.append()` паттерн |
| USE_SUPABASE — _shifts_load() через StorageProvider | ✅ — существующий вызов, не изменён |

---

## 5. Definition of Done

| Критерий | Выполнен |
|----------|----------|
| `chat_with_bot()` обогащает контекст nickname/squad_name/shift_name | ✅ |
| Системный промпт использует эти поля если они есть | ✅ |
| Smoke Flow G: 2 checks (G-1, G-2) реализованы | ✅ |
| `BACKEND_CONTRACT_GUARD.md` обновлён §3.5 + §5 | ✅ |
| Синтаксис всех файлов: 0 ошибок | ✅ |

---

## 6. Files Changed

| Файл | Изменение |
|------|-----------|
| `backend/app.py` | Enrichment block в `chat_with_bot()` |
| `chatbot/models/conversation.py` | +3 поля в `WebContext` |
| `chatbot/core/context_manager.py` | +3 строки в `update_web_context()` |
| `chatbot/prompts/system_prompt.py` | +3 kwargs + context_parts + hint |
| `chatbot/core/response_generator.py` | +3 kwargs во всех 7 call sites |
| `backend/scripts/smoke_backend_critical.py` | `run_flow_g()` + wire into `run()` |
| `docs/BACKEND_CONTRACT_GUARD.md` | §1 + §3.5 + §5 (Flow G, 43 checks) |

---

## 7. Handoff

**DevBro / Frontend:**
- Никнейм можно передавать в JWT payload (`nickname` field) — бот автоматически подхватит
- `squad_name` и `shift_name` обогащаются сервером из membership — клиент ничего менять не должен
- Структура ответа `/api/chat` не изменилась

**Ops / NeuroStepa:**
- Если JWT не содержит `nickname`, бот не будет обращаться по нику (graceful degradation)
- Если у пользователя нет membership record или squad — `squad_name`/`shift_name` остаются `None`, промпт не добавляет эти строки
- Smoke Flow G требует рабочего `/api/auth/verify-code` + `AUTH_SECRET` для полного прогона
