# ROADMAP 2026 — единая точка входа для агентов

Источники: [REPORT_2026.md](../REPORT_2026.md), [ANALYSIS_AND_VISION_2026.md](../ANALYSIS_AND_VISION_2026.md), [CODEX_ANALYSIS_2026-02-04.md](../CODEX_ANALYSIS_2026-02-04.md), [STEPA_VISION_LC.md](STEPA_VISION_LC.md) (видение по механикам ЛК). При расхождении приоритетов — [WORKFLOW_GAME_CONCEPT_PLAN.md](../WORKFLOW_GAME_CONCEPT_PLAN.md). Детальный прогресс и матрица планов: [WORKFLOW_PROGRESS_AND_PLANS.md](../WORKFLOW_PROGRESS_AND_PLANS.md).

---

## Для агентов (точки входа и страховка)

1. **Перед реализацией любой задачи из этого файла:** (1) Проверь статус пункта в таблице ниже. (2) Если статус **Done** — не реализуй заново; при необходимости открой Evidence и убедись в коде/доках. (3) Текущий фокус — секция «Где мы сейчас»; бери следующую задачу оттуда или из пунктов со статусом Not started по приоритету.
2. **Детальный лог выполненного** — в [.memory-bank/progress.md](../.memory-bank/progress.md).

---

## Где мы сейчас

**Текущий фокус:** Песочница — Done. Auth flow — Done. Роли, авторизация, адаптация ЛК — Done (родитель как участник, лимит с бэкенда, GET /api/chat/limits). Учёт лимита сообщений чата на бэкенде (429) — Done. Slice 4 «Мой арт» (локальные скины) — Done. Просмотр прогресса ребёнка родителем по коду/ссылке (Phase 1: файл и ссылка `parent_view`; Phase 2: короткий код, API, QR) — Done. **CI:** перед сборкой выполняется self-check (в job lint-and-test); отдельный job backend-health поднимает бэкенд, ждёт ответа GET /api/health и при успехе запускает self-check с BACKEND_URL — Done.  
**Следующая конкретная задача:** Выбрать из видения и планов: [STEPA_VISION_LC.md](STEPA_VISION_LC.md), [FEATURE_AUTH_ROLES_DVIZHKI_PLAN.md](FEATURE_AUTH_ROLES_DVIZHKI_PLAN.md), [WORKFLOW_GAME_CONCEPT_PLAN.md](../WORKFLOW_GAME_CONCEPT_PLAN.md) — или новая инициатива. Следующие кандидаты: онлайн-Движки (расширение), Герб Движка шаг 2 (backend ИИ). **Создание смен/отрядов организатором (staff flow MVP)** — Done. **UX-шлифовка блока «Смены и отряды»** — Done. **Консистентность пустых состояний/ошибок в блоках ЛК** — Done. **Унификация ЛК**, **Серверный RBAC для чата**, **Shift_leader и отряд из вожатых (MVP)** — Done. **Архитектура:** Vercel + cf-api — см. [ARCHITECTURE_AND_RESOURCES.md](ARCHITECTURE_AND_RESOURCES.md).

---

## Архитектура и ресурсы

**Подключённые ресурсы:**

| Ресурс | Назначение | Триггер |
|--------|------------|---------|
| **Vercel: rl-guide-book** | Фронтенд | Push main |
| **Vercel: backend** | API (чаты, community, будущее: auth) | Push main |
| **cf-api (Cloudflare)** | Боты VK/TG (автокомменты, обсуждения) | Ручной/CI |

**Разделение ответственности:**
- **Vercel** — фронт + backend для авторизации, синхронизации, community badges, роли.
- **cf-api** — специализированная логика ботов (NeuroValyusha в соцсетях), Cloudflare KV.
- **Детали:** [ARCHITECTURE_AND_RESOURCES.md](ARCHITECTURE_AND_RESOURCES.md), Cursor plan Centralize Bot Backend Migration.

**Целевые роли (реализовано):** Путешественник, Участник смены, Родитель, Вожатый (Реальный вожатый), Руководитель смены (Старший Вожатый), Разработчик — см. [FEATURE_AUTH_ROLES_DVIZHKI_PLAN.md](FEATURE_AUTH_ROLES_DVIZHKI_PLAN.md), [AGENT_REPORT_ORGANIZER_ROLE.md](AGENT_REPORT_ORGANIZER_ROLE.md).

---

## Таблица инициатив

| Статус       | Название | Evidence |
| ------------ | -------- | -------- |
| Done         | P1-01: Supabase schema v1 | `backend/migrations/001_schema_v1.sql` — 9 таблиц (shifts, squads, memberships, squad_corners, squad_invite_codes, squad_messages, badge_requests, parent_snapshots, chat_daily_usage), индексы, constraints, retention trigger. |
| Done         | P1-02: StorageProvider + Supabase provider | `backend/storage/` — 4 файла (base.py, json_provider.py, supabase_provider.py, __init__.py); все 8 пар _xxx_load/_xxx_save в app.py переключены на get_store(); USE_SUPABASE=false → JSON (local dev), USE_SUPABASE=true → Supabase (prod). |
| Done         | P1-03: Supabase для badge_requests/parent_snapshots/chat_daily_usage | Реализовано в составе P1-02; все 8 сторов (включая BadgeRequestsStore, ParentSnapshotsStore, ChatDailyUsageStore) присутствуют в обоих провайдерах. |
| Done         | P1-05: Убрать forced traveler в prod | [src/utils/authStorage.ts](../src/utils/authStorage.ts): удалён блок `import.meta.env.PROD && role !== 'traveler'`; роль берётся из JWT напрямую; при expired/отсутствующем токене DEFAULT_ROLE='traveler' → FeatureGate CTA «Разблокировать по коду». |
| Done         | P1-07: Rate limits + safety-фильтры чата | `backend/app.py` строки 103–180: `_validate_squad_message` (URL-фильтр, длина 500, мат-фильтр), `_check_squad_msg_rate_limit` (per-device per-min), `_check_chat_per_min_rate_limit`, `_log_rate_limit_event` (hashed deviceId); подключено в `/api/squads/<id>/messages` и `/api/chat`. `.env.example`: `SQUAD_MSG_MAX_LEN`, `SQUAD_MSG_RATE_LIMIT`, `CHAT_MSG_RATE_LIMIT_PER_MIN`. |
| Done         | P1-08: Единый контур чата через backend | `src/utils/aiService.ts`: добавлены `getChatEndpoint()`, `getChatLimitsEndpoint()` (через `VITE_BACKEND_URL` в prod); убраны 8 мест с `real-vibe-ai-studio.pages.dev`; `src/components/ChatBot.tsx`: 2 места заменены; grep `real-vibe-ai-studio` в `src/` → 0 совпадений. `.env.example`: `VITE_BACKEND_URL`. |
| Done         | P1-09: UX smoke-сценарии и доводка | [src/views/ProfileView.tsx](../src/views/ProfileView.tsx): панель «Войти по коду» (было «Разблокировать бота»), закрывается после verify-code + showHint «Добро пожаловать»; аудит P0-сценариев (unlock→отряд→чат, заявка→approve, parent QR) — все покрыты в коде; баги B-01/B-02/B-03 исправлены. |
| Done         | P1-04: Закрыть dev-двери в production | `backend/app.py`: `_is_production()` + `/api/dev/login` -> 404 при ENVIRONMENT=production; `src/views/ProfileView.tsx` showSandbox=!import.meta.env.PROD. `.env.example` + ENVIRONMENT. |
| Done         | P1-06: Server-side RBAC по JWT | `backend/app.py`: аудит всех endpoint'ов — все используют `_require_roles()`/`_require_organizer_jwt()` из JWT; JWT expiry -> 401, без токена -> 401. |
| Done         | P1-10: Docs: staging checklist + monitoring | `docs/PROD_RELEASE_PLAYBOOK.md` §5.1 curl smoke tests + §7 monitoring table; `docs/CAMP_RUNBOOK.md` §6.1–6.5 incident runbook (топ-3 инцидента). |
| Done         | P2-01: RBAC для educator | `backend/app.py`: educator в CHAT_ALLOWED_ROLES; badge inbox/approve/reject; shifts+squads read; squad join+messages; ORGANIZER_ROLES не изменён — educator не может создавать/удалять. |
| Done         | P2-02: Дашборды staff | [src/views/ProfileView.tsx](../src/views/ProfileView.tsx): `inboxStatusFilter` state, фильтры (Все/Ожидают/Одобрены/Отклонены) + статистика в блоке eventsTab=approvals; участники без raw deviceId + pending badge count; educator добавлен в squad join form. |
| Done         | P2-04: Кабинет педагога v1 | [src/components/EducatorCabinetPanel.tsx](../src/components/EducatorCabinetPanel.tsx) — новый компонент (3 вкладки: Задания/Расписание/Группы, localStorage `rl_educator_cabinet_v1`); [src/views/ProfileView.tsx](../src/views/ProfileView.tsx) — PanelViewId, panelTitleMap, nav 📚, render при educator/camp_director/developer. |
| Done         | Среда и доки (SERVERS.md, порты) | [SERVERS.md](../SERVERS.md): секция «Dev-сервер (Vite)», порт по умолчанию 3001, допустим любой (например 3009). |
| Done         | Первый вход в ЛК (4 шага туториала) | [ProfileView.tsx](../src/views/ProfileView.tsx): `startTutorial` с 4 шагами; селекторы `#profile-passport-card`, `#profile-tab-active`, `#profile-tab-collection`, `#profile-chat-trigger`. [HintOverlayContext.tsx](../src/context/HintOverlayContext.tsx): вызов `onComplete` в `hideHint`. `userData.meta.hasCompletedTutorial`. |
| Done         | Резервная копия в ProfileView | [ProfileView.tsx](../src/views/ProfileView.tsx): блок с текстом «Прогресс хранится на этом устройстве», кнопка «Сделать резервную копию», «Восстановить из файла». |
| Done         | Тестовый режим (только для разработки) | [.memory-bank/tech_context.md](../.memory-bank/tech_context.md): секция «Development: Test Mode». [README.md](../README.md): раздел «Разработка», абзац про `rl_guide_test_mode`. |
| Done         | CODEX Step A (доки/окружения) | Совпадает со «Среда и доки». |
| Done         | CODEX Step B (резервная копия, тест-режим) | Совпадает с пунктами «Резервная копия» и «Тестовый режим». |
| Done         | CODEX Step C (Share MVP) | Share Center в ProfileView; CTA «Поделиться прогрессом» / «Создать Движок»; тост после «В путь» в BadgeView с переходом в Share Center. |
| Done         | Share Center (форматы, тумблер, пригласить друзей, экспорт/импорт) | [ProfileView.tsx](../src/views/ProfileView.tsx): Share Center — 9:16 и 16:9, тумблер «Скрыть ник», `shareOrDownloadSocialCard`, блок «Пригласить друзей», кнопки экспорт/«Восстановить из файла», автоскролл к #share/#share-center. |
| Done         | Мастерская: разблокировка по 1.16.1/1.16.2, параметризация по категории, триггеры | A1: вкладка Мастерская разблокируется при 1.16.1 или 1.16.2 в пути/достигнуты. A2: BadgeView/CategoryView — «Предложи свой вариант» → profile #workshop?categoryId=…. A3: categoryId из URL в форме. ProfileView открывает workshop по #workshop. |
| Done         | Мастерская: полный флоу после создания значка | ProfileView: подтверждение «Концепт успешно выкован!», Telegram, карточка «АКТИВИРОВАТЬ В ПУТЕВОДИТЕЛЕ» / «ГЕНЕРИРУЕМ...». |
| Done         | Part B: Мои предложения, Rank Up, sync ai-data | B1: вкладка Мастерская — блок «Мои предложения», `customBadges`, «Отправить в сообщество». B2: Rank Up оверлей при смене ранга, `markRankUpSeen`. B3: `npm run sync:ai-data`, [docs/DATA_SYNC.md](DATA_SYNC.md). |
| Done         | Slice 2: Social/Viral MVP | SocialGenerator.ts (9:16, 16:9), Share Preview в ProfileView, fetchAiSlogan. |
| Done         | Slice 3: Smart Onboarding | HintOverlayContext, SmartHint (SVG mask, highlight), 4-шаговый туториал в ProfileView, scroll lock при подсказке. |
| Done         | Slice 4: Creator Workshop | Форма создания значка в Profile, 4К-навыки, интеграция с Telegram. |
| Done         | Sprint 1: Инкубатор — загрузка community_badges, глобальная лента в UI | [useDataLoader.ts](../src/hooks/useDataLoader.ts): `COMMUNITY_BADGES_CACHE_KEY`, загрузка из localStorage затем fetch(COMMUNITY_API_URL), запись кэша; возврат `communityBadges`. [ProfileView.tsx](../src/views/ProfileView.tsx): блок «Идеи Сообщества» во вкладке Мастерская (workshop-community-feed), список `communityBadges`. |
| Done         | Техдолг: валидация community_badges на бэкенде; оффлайн-синхронизация localStorage | [backend/app.py](../backend/app.py): `ensure_json_files()` вместо ensure_community_file; `_validate_community_badge`, `_check_community_rate_limit` (5 POST/мин по IP); санитизация title/description/id. [useDataLoader.ts](../src/hooks/useDataLoader.ts): `COMMUNITY_PUBLISH_QUEUE_KEY`, очередь при offline, обработчик `online`, `processCommunityQueue`; возврат `{ ok, queued }` из publishBadgeToCommunity; ProfileView — подсказка «Будет отправлено при появлении сети», индикация Оффлайн/Синхронизация/В очереди. |
| Done         | Мастерская до продакшн-уровня | Ошибки: [useDataLoader.ts](../src/hooks/useDataLoader.ts) — publishBadgeToCommunity возвращает error/status при 429/400/5xx; ProfileView — showHint при ошибке отправки в сообщество, тост при 503 notify-creator-card. UX: confirm заменён на модалку «Концепт выкован» (две кнопки). Консистентность: id workshop-section-community-locked в locked-ветке, .profile-empty-state и .sr-only, a11y (labels, aria-label, aria-live). Данные: экспорт/импорт customBadges в резервной копии (exportData(extras), importData возвращает data, restoreCustomBadges), подсказка в блоке «Резервная копия». Продукт: удаление «Моих предложений» (removeCustomBadge, кнопка «Удалить»), DEFAULT_WORKSHOP_CATEGORY_ID, подсказка про категорию в Кузнице. Доки: [.memory-bank/tech_context.md](../.memory-bank/tech_context.md) — контракт Community badges (429/400), раздел «Мастерская (ЛК)». |
| Done         | Sprint 2: рефакторинг прожектора, баги позиционирования на главной | [BlueNestLanding.tsx](../src/components/BlueNestLanding.tsx): позиционирование всплывашки «О концепции игры» от триггера через `conceptButtonRef.getBoundingClientRect()`; state `conceptPopoverAnchor`, `updateConceptPopoverPosition()`; портал с `top`/`left` и `transform: translate(-50%, 0)`; подписка на `resize` при открытой всплывашке. |
| Done         | Sprint 3: социальный мост (карточки достижений с ИИ, «Поделиться» со ссылкой) | [socialGenerator.ts](../src/utils/socialGenerator.ts): `getBadgeShareUrl(badgeId)` — URL `?view=badge&badgeId=...`. [BadgeView.tsx](../src/views/BadgeView.tsx): кнопка «Ссылка на значок» в тосте после «В путь», копирование ссылки в буфер. [BadgeLevelView.tsx](../src/views/BadgeLevelView.tsx): кнопка «Ссылка на значок» в блоке шаринга достижения. Открытие по ссылке — useAppController URL deep-links (`view=badge&badgeId`). Карточки достижений с ИИ уже в BadgeLevelView (fetchAiSlogan, achieved_level). |
| Done         | Качество шеринга: вирусный контент, вайб Путеводителя, CTA и хештеги | [socialGenerator.ts](../src/utils/socialGenerator.ts): отрисовка `customCaption` на карточке (акцентный цвет, до 2 строк); CTA «Присоединяйся к Путеводителю» и хештеги в футере; челлендж-фразы по seed («Бро, твой ход!», «Попробуй обойти», «Сколько уровней закроешь ты?»); `buildCaption` дополняет слоган призывом и хештегами. [aiService.ts](../src/utils/aiService.ts): расширенные промпты с вайбом, сленгом, контекстом (ранг, achieved/started); [ProfileView.tsx](../src/views/ProfileView.tsx): передача totalLevelsAchieved/totalBadgesStarted в fetchAiSlogan. |
| Done         | Карточка прогресса AAA (шеринг по спекам) | [socialGenerator.ts](../src/utils/socialGenerator.ts): для `kind === 'progress_summary'` отдельный pipeline `drawProgressSummaryAAA` по §9: 1) BG 2) FRAME 3) BRAND+PILL 4) AVATAR 5) RANK 6) STATS 7) SLOTS 8) BUFF 9) FOOTER; anchor-driven лейаут (ProgressAnchors A, отрисовка по A.*); fitFontSize + лента ранга; два чипа статов; футер — только CTA, хештеги, tagline (до 60 символов), дата; без диагностического маркера. [docs/AAA_PROGRESS_CARD_SPEC.md](AAA_PROGRESS_CARD_SPEC.md) — источник истины. |
| Done         | Аналитика 4К (профиль 4К + Программа 2026) | [profile4k.ts](../src/utils/profile4k.ts), [Profile4KDashboard.tsx](../src/components/Profile4KDashboard.tsx): 4К-навыки, маппинг категорий, Программа Реального Лагеря 2026 (soft skills, вожатство, нейросети). |
| Done         | Реальный Отряд: расширение RealDiaryDashboard и логики отряда | [RealDiaryDashboard.tsx](../src/components/RealDiaryDashboard.tsx): Реальный Дневник (заголовок), презентация с данными отряда, связь с Инспектором (скролл + подсказка), тост «Скопировано». |
| Done         | Подробная анкета при подтверждении значка: опыт, влияние, ссылка, фото | [ProfileView.tsx](../src/views/ProfileView.tsx): proofForm с секциями Опыт/Реальный вклад/Ссылка/Фото, методическая подсказка, мягкая валидация при пустой форме, тост после отправки, закрытие по overlay. |
| Done         | Методика: наполнение Архитектора Отряда реальными сценариями | [SquadArchitect.tsx](../src/components/SquadArchitect.tsx): 16 традиций (10 базовых + 6 из Путеводитель.md), duration/materials/counselorTip, кнопка «Копировать сценарий» + тост, сохранение в `meta.squadArchitectScenario` (saveSquadArchitectScenario), `diarySquadName` из Реального Дневника, showHint вместо alert. [ProgressContext.tsx](../src/context/ProgressContext.tsx), [userProgress.ts](../src/types/userProgress.ts), [ProfileView.tsx](../src/views/ProfileView.tsx). |
| Done         | Совет Лагеря: развитие CouncilDashboard | [CouncilDashboard.tsx](../src/components/CouncilDashboard.tsx): обзор инициатив и протоколов (цикл, типичные инициативы), связь с Движками (useTeam, скролл к team-dashboard). |
| Done         | P2-03: Совет Лагеря — персистентный список инициатив | `backend/storage/base.py`: `CouncilInitiativesStore`. `backend/storage/json_provider.py`: `JsonCouncilInitiativesStore`. `backend/storage/supabase_provider.py`: `SupabaseCouncilInitiativesStore`. `backend/migrations/002_council_initiatives.sql`: DDL + индекс + CHECK + RLS. `backend/app.py`: `GET/POST /api/council/initiatives` (auth CHAT_ALLOWED_ROLES). [CouncilDashboard.tsx](../src/components/CouncilDashboard.tsx): форма «💡 Предложить» + живой список со статус-бейджами (idea/discussion/decided/done). |
| Done         | Этап 5: Синхронизация с чатом | Экран «Личный кабинет» в `current_view` (ProfileView); маппинг `profile`→«Личный кабинет» в generateWhereAmI (backend, api-standalone, Python chatbot), пункт в системном промпте «Как я адаптируюсь», ветка profile в analyzeRequestType; ответы бота проверены, приёмка пользователем 2026-02-07. |
| Done         | Этап 6: Устранение хардкода чисел/дат | useDataLoader возвращает masterIndex; BlueNestLanding — totalBadges/totalCategories из индекса, carousel без slice(0,14); CategoriesGrid — без лимита 14; AboutCampView — год из lastUpdated; ProfileView — «Данные актуальны на {lastUpdated}»; BroInitiation — COUNSELOR_LEVEL_THRESHOLD; Profile4KDashboard — getProgram2026CategoryIds(). |
| Done         | CategoriesGrid: подсказка о прогрессе | [CategoriesGrid.tsx](../src/components/CategoriesGrid.tsx): в hero-карточке под маркировкой строка «N из M уровней закрыто» или «Закрыто уровней: N»; `masterIndex` передаётся из AppViewRouter. [categories.css](../src/styles/categories.css): `.categories-progress-hint`. |
| Done         | CategoryView: фильтры по статусу | [CategoryView.tsx](../src/views/CategoryView.tsx): вкладки «Все» / «Мои» / «В процессе» / «Получены»; `filteredBadges` по `getBadgeProgress`; пустое состояние при 0 результатах. [category-view.css](../src/styles/category-view.css): `.category-badge-filters`, `.category-filter-btn`. |
| Done         | Этап 7: отправка в Telegram-канал при подтверждении уровня | Тот же TELEGRAM_BOT_TOKEN и TELEGRAM_CHANNEL_ID из .env. [backend/app.py](../backend/app.py): `send_telegram_message()`, POST `/api/telegram/notify-achievement` (levelId, levelLabel, reflection, impact, link). [ProfileView.tsx](../src/views/ProfileView.tsx): при «Отправить в Telegram» вызов API и тост; при ошибке — fallback на ручную отправку (t.me/Stivanovv). |
| Done         | ЛК: Предложить инициативу в совет лагеря | [aiService.ts](../src/utils/aiService.ts): `fetchCouncilInitiative`. Кнопка в CouncilDashboard, TeamDashboard, WingDashboard; модалка в ProfileView (форма + генерация + копировать/Telegram). |
| Done         | ЛК: Создать движок — QuotaExceededError и slim-сериализация | [TeamContext.tsx](../src/context/TeamContext.tsx): try/catch при setItem, alert при QuotaExceededError; `slimTeamForStorage` убирает data URL из logo/flagImage/members[].avatar. Защита userData.profile в createTeam/handleCreate. |
| Done         | ЛК: Штаб Крыла всегда виден, вход «Сформировать Крыло» | [WingDashboard.tsx](../src/components/WingDashboard.tsx): без раннего return null; при !bro?.wingId — карточка с кнопкой «Сформировать Крыло» (selectWingMentor('', 'Моё Крыло')); при wingId — полный блок с аватаром и «Добавить фото». |
| Done         | Значки на флаг отряда (категория 10) | [userProgress.ts](../src/types/userProgress.ts): `flagBadgeRequests`, `flagBadgesApproved` в `diaryProgress.squad`. [ProgressContext.tsx](../src/context/ProgressContext.tsx): `addFlagBadgeRequest`, `approveFlagBadgeRequest`, `rejectFlagBadgeRequest`; нормализация squad при гидрации. [SquadCornerDashboard.tsx](../src/components/SquadCornerDashboard.tsx): секция «Значки на флаг отряда» — на флаге (10.1–10.3), заявки; кнопка «Отправить все заявки на рассмотрение» (ссылка COUNSELOR_TELEGRAM с объединённым текстом pending заявок); подзаголовок «Заявки на рассмотрение» и пояснения в пустом состоянии. [BadgeView.tsx](../src/views/BadgeView.tsx): при «В путь» для 10.x — addFlagBadgeRequest. [BadgeLevelView.tsx](../src/views/BadgeLevelView.tsx): при достижении 10.x.1 — addFlagBadgeRequest с evidence (reflection). |
| Done         | ЛК: загрузка фото — Движок (флаг), Отрядный уголок (4 фото) | [TeamDashboard.tsx](../src/components/TeamDashboard.tsx): загрузка фото флага отряда (`teamPhoto`, updateTeam). [SquadCornerDashboard.tsx](../src/components/SquadCornerDashboard.tsx): 4 поля фото (уголок, флаг, отряд, с вожатыми) — PHOTO_FIELDS, handlePhotoChange, handleSaveSquad. [userProgress.ts](../src/types/userProgress.ts), [ProgressContext.tsx](../src/context/ProgressContext.tsx). Штаб Крыла (аватар) — см. строку «ЛК: Штаб Крыла». |
| Done         | План получения значка: качественная генерация и UX «Дополнить» | [ProfileView.tsx](../src/views/ProfileView.tsx): levelId при setPlanFormBadge, nameExplanation/skillTips/confirmation; кнопка «Дополнить» на result, «Изменить контекст»; подсказка при пустом контексте; planStep='result'. [aiService.ts](../src/utils/aiService.ts): BadgePlanInput расширен, промпт с блоками значка, programHint, логирование camp-program. [chatbot/core/response_generator.py](../chatbot/core/response_generator.py): max_tokens 2400, детекция badge_plan. |
| Done         | Лента с лайками (Идеи Сообщества) | [useDataLoader.ts](../src/hooks/useDataLoader.ts): `COMMUNITY_LIKES_KEY`, `getCommunityLikes`, `setCommunityLikes`, state `communityLikedIds`, `toggleCommunityLike`; [ProfileView.tsx](../src/views/ProfileView.tsx): кнопка-лайк (Icons.Heart) у каждой идеи в блоке «Идеи Сообщества», сохранение в localStorage. |
| Done         | Формат событий и Webhook (Telegram) | [backend/events.py](../backend/events.py): ConfirmationRequestedEvent, LevelAchievedEvent, append_confirmation_event, get_confirmation_events. [backend/app.py](../backend/app.py): POST /api/webhook/telegram/<secret>, _handle_telegram_webhook_payload, сохранение в confirmation_events.json; GET /api/webhook/confirmation-events; send_telegram_message. [scripts/set_telegram_webhook.py](../scripts/set_telegram_webhook.py). [docs/EVENTS_AND_WEBHOOKS.md](EVENTS_AND_WEBHOOKS.md). |
| Done         | Ответ бота при приёме заявки | [backend/app.py](../backend/app.py): send_telegram_to_chat, вызов в _handle_telegram_webhook_payload после append_confirmation_event; текст «Заявка принята, вожатый посмотрит». |
| Done         | Webhook VK | [backend/app.py](../backend/app.py): POST /api/webhook/vk/<secret>, send_vk_message, _handle_vk_webhook_payload; обработка confirmation и message_new; ответ пользователю «Заявка принята, вожатый посмотрит». [docs/EVENTS_AND_WEBHOOKS.md](EVENTS_AND_WEBHOOKS.md) §4. [scripts/print_vk_webhook_url.py](../scripts/print_vk_webhook_url.py). |
| Done         | Архитектура ресурсов: централизация cf-api | cf-api вынесен в cf-api/; npm run sync:cf-api-ai-data; [docs/DEPLOY_CF_API.md](DEPLOY_CF_API.md); .gitignore не исключает cf-api; сборка работает. [ARCHITECTURE_AND_RESOURCES.md](ARCHITECTURE_AND_RESOURCES.md). |
| Done         | Песочница для тестирования | Генератор кодов, dev-одобрения, панель входящих заявок. [ProfileView.tsx](../src/views/ProfileView.tsx): `showSandbox` (DEV или `?sandbox=1`), блоки «Сгенерировать код подтверждения», «Входящие заявки», «Dev: быстрые одобрения». [backend/app.py](../backend/app.py): POST `/api/auth/generate-code`, POST `/api/auth/verify-code`. [docs/SANDBOX_TESTING.md](SANDBOX_TESTING.md). Auth: traveler lock, JWT, Authorization в чат. |
| Done         | Роли, авторизация, адаптация ЛК | MVP: [authRole.ts](../src/types/authRole.ts) — canSeeOtradBlocks, isReadOnlyRole, showEventsPanelForRole. [ProfileView.tsx](../src/views/ProfileView.tsx): скрытие отрядных блоков для traveler; родитель играет как участник (кнопки редактирования не удаляются); панель «Входящие заявки» для counselor/shift_leader без sandbox. [ChatBot.tsx](../src/components/ChatBot.tsx): подстановка лимита с бэкенда (GET /api/chat/limits). [backend/app.py](../backend/app.py): CHAT_MESSAGES_PER_DAY, GET /api/chat/limits. [FEATURE_AUTH_ROLES_DVIZHKI_PLAN.md](FEATURE_AUTH_ROLES_DVIZHKI_PLAN.md). |
| Done         | Кабина космического корабля (ЛК) | [ProfileView.tsx](../src/views/ProfileView.tsx): cabin layout (`profile-view-cabin-layout`, `panelActiveView`, `openCabinPanel`), панели Инспектор/Паспорт/Отряд вожатых/Отрядный уголок/Реальный Дневник/Движок/Совет/БРО/Мастерская/Шеринг/Вожатификатор/4К/Для родителей; нижняя консоль с кнопками и терминалом. [profile-view-spaceship.css](../src/styles/profile-view-spaceship.css), [main.tsx](../src/main.tsx) — `profile-spaceship-root`. Сервер кабины: порт 3010, [profile-desktop.html](../profile-desktop.html). [PROFILE_CABIN_SERVER_SETUP.md](PROFILE_CABIN_SERVER_SETUP.md). |
| Done         | Унификация личного кабинета | Единая точка входа: в [main.tsx](../src/main.tsx) обёртка `profile-spaceship-root` и импорт profile-view-spaceship.css — кабина во всех режимах. [profile-desktop.html](../profile-desktop.html) переведён на main.tsx; main-profile-desktop.tsx удалён. Vite: multi-page build (dist — index.html и profile-desktop.html), dev-плагин для /RL-Guide-book/profile-desktop.html. Консоль через spaceship.css; в profile-view.css убрано display:none для .profile-view-console. ЛК с кабиной и пузырями единообразен на 3001, 3010, 3002. Обновлены [PROFILE_CABIN_SERVER_SETUP.md](PROFILE_CABIN_SERVER_SETUP.md), [SERVERS.md](../SERVERS.md). |
| Done         | Серверный RBAC для чата | [backend/app.py](../backend/app.py): `CHAT_ALLOWED_ROLES`; `_require_chat_auth()` — проверка Authorization: Bearer, JWT decode, роль в CHAT_ALLOWED_ROLES; вызов в начале `chat_with_bot()` (401/403 при ошибке). [ChatBot.tsx](../src/components/ChatBot.tsx): всегда отправлять `Authorization` при accessToken; обработка 401 (сообщение + clearAuth) и 403 (сообщение). [.memory-bank/tech_context.md](../.memory-bank/tech_context.md): контракт POST /api/chat. |
| Done         | Shift_leader и отряд из вожатых (MVP) | [authRole.ts](../src/types/authRole.ts): shift_leader, canCreateCounselorSquad. [CounselorSquadContext.tsx](../src/context/CounselorSquadContext.tsx): createSquad, joinByCode, inviteCode/link, ?counselor_squad. [ProfileView.tsx](../src/views/ProfileView.tsx): панель counselor-squad — создание отряда для shift_leader/developer, вход по коду для counselor. Backend: CHAT_ALLOWED_ROLES с shift_leader, verify-code выдаёт JWT. [SANDBOX_TESTING.md](SANDBOX_TESTING.md) §6. |
| Done         | Обмен итогами Реального Дневника | [RealDiaryDashboard.tsx](../src/components/RealDiaryDashboard.tsx): кнопка «Отправить в Telegram» рядом с «Копировать»; `handleSendToTelegram` — формирование текста через `buildPresentationText()`, открытие `t.me/Stivanovv?text=...` в новой вкладке; тост «Открыт Telegram для отправки»; fallback при ошибке — копирование в буфер; кнопка отключена при пустых/placeholder данных. Паттерн как в proofForm и модалке инициатив. |
| Done         | ИИ-Манифест Маршрута | [BadgeView.tsx](../src/views/BadgeView.tsx): после «В путь» тост «Поделиться» → handleShareStart: навык из [profile4k.ts](../src/utils/profile4k.ts) (getBadge4kSkills, getSkillLabel), вызов из [aiService.ts](../src/utils/aiService.ts) (kind route_manifest_challenge), manifestCaption «Мой вызов на сегодня — …»; [socialGenerator.ts](../src/utils/socialGenerator.ts): manifestSkill в input, при start_route+manifestSkill — sub «Я выбираю путь X, чтобы прокачать Y.», customCaption до 120 символов / 3 строк. Fallback при отсутствии ИИ. |
| Done         | Учёт лимита сообщений чата на бэкенде | [backend/app.py](../backend/app.py): `_check_and_inc_chat_daily(device_id)` — счётчик по дням (UTC) в `backend/data/chat_daily_usage.json`, lock при read-modify-write; вызов в `chat_with_bot()` после auth при непустом `deviceId` из JWT; при превышении CHAT_MESSAGES_PER_DAY — 429 `{"error": "Daily limit exceeded", "retryAfter": "tomorrow"}`. [ChatBot.tsx](../src/components/ChatBot.tsx): обработка 429 — сообщение «Лимит сообщений на сегодня исчерпан.» (или errData.error), без clearAuth(). [.memory-bank/tech_context.md](../.memory-bank/tech_context.md): контракт 429 для POST /api/chat. |
| Done         | Связность Дневник ↔ Инспектор | [RealDiaryDashboard.tsx](../src/components/RealDiaryDashboard.tsx): `getDiaryInspectorHint` — подсказки по inspectorProgress.currentDay и diary entries (Миссия Дружбы/Вежливости/Уюта). [InspectorDashboard.tsx](../src/components/InspectorDashboard.tsx): `onOpenDiary`, карточка «Перед миссией полезно записать рефлексию…» + «Открыть Дневник». [ProfileView.tsx](../src/views/ProfileView.tsx): onScrollToInspector + setActiveTab('active'); onOpenDiary в оба InspectorDashboard. |
| Done         | Карточка Созидателя 9:16 → модераторам | После создания значка в Мастерской (Кузница Смыслов): карточка 9:16 с текстом «Я предлагаю новый смысл: Значок [Название]. Кто за?» ([socialGenerator.ts](../src/utils/socialGenerator.ts) — вид `creator_proposal`), отправка в Telegram ([backend/app.py](../backend/app.py): `send_telegram_photo`, POST `/api/telegram/notify-creator-card`), затем shareOrDownloadSocialCard. [ProfileView.tsx](../src/views/ProfileView.tsx): handleWorkshopSubmit с kind `creator_proposal`, фоновый вызов notify-creator-card, тост «Карточка отправлена вожатым». |
| Done         | Slice 4: Мой арт (локальные скины) | [userProgress.ts](../src/types/userProgress.ts): `customBadgeImages`; [ProgressContext.tsx](../src/context/ProgressContext.tsx): `setCustomBadgeImage`, миграция data URL из selectedSkins. [BadgeIcon.tsx](../src/components/BadgeIcon.tsx): baseBadgeId, приоритет custom/legacy. [BadgeView.tsx](../src/views/BadgeView.tsx): переключатель «Мой арт» / «Загрузить свой арт» / «Удалить арт», hero по custom. [BadgeLevelView.tsx](../src/views/BadgeLevelView.tsx): hero по custom. [ProfileView.tsx](../src/views/ProfileView.tsx): загрузка изображения в форме Кузницы Смыслов, setCustomBadgeImage при сабмите. [.memory-bank/tech_context.md](../.memory-bank/tech_context.md): секция «Мой арт». |
| Done         | Онлайн-Движки (membership, синхронизация с бэкендом) | Членство и состав на бэкенде: [backend/app.py](../backend/app.py) — `_require_teams_auth()`, GET `/api/teams/mine`, POST `/api/teams` (создание без id), POST `/api/teams/:id/join`, POST `/api/teams/:id/leave`, PATCH/DELETE с проверкой лидера. [TeamContext.tsx](../src/context/TeamContext.tsx): загрузка mine при mount при accessToken, create/join/leave/update/delete через API с кэшем в localStorage; без токена — только локальный режим. [TeamDashboard.tsx](../src/components/TeamDashboard.tsx): обработка 404/409/401 через showHint, передача nickname/avatar в joinTeam, currentUserId по deviceId при auth. Путешественник — по-прежнему только локальный Движок. |
| Done         | Онлайн-Движки (расширение): предпросмотр по коду | [TeamDashboard.tsx](../src/components/TeamDashboard.tsx): при вводе кода — debounced (400 ms) GET `/api/teams/:id`, отображение превью (название, девиз) или «Код не найден»; при открытии ссылки с `?engine=<base64>` — декод приглашения, подстановка кода и превью без запроса, блок «Войти по коду» раскрыт. State: joinPreview, joinPreviewLoading, joinPreviewError; сброс превью при успешном вступлении. |
| Done         | Онлайн-Движки (расширение): UX приглашений и участников | [TeamDashboard.tsx](../src/components/TeamDashboard.tsx): кнопка «Ссылка» рядом с «Код Движка» — копирование `generateInviteUrl()` в буфер и тост showHint «Ссылка скопирована»; копирование кода (кнопка «+») — showHint вместо alert; блок ЭКИПАЖ — аватар через `isImageUrl(member.avatar)` и `<img>` при URL/data URL, иначе эмодзи; подпись с nickname под кружком; aria-label для кнопок ВЫЙТИ, УДАЛИТЬ, «+», «Ссылка». |
| Done         | Родитель по коду (Phase 1 + Phase 2) | Phase 1: [ProfileView.tsx](../src/views/ProfileView.tsx) — «Создать отчёт для родителя» (скачивание JSON, копирование ссылки `?parent_view=`), модалка «Значки моего ребёнка» (загрузка файла, «Открыть по ссылке»), обработка `?parent_view=` при загрузке; [userProgress.ts](../src/types/userProgress.ts) — `ParentReportPayload`, `buildParentReportPayload`. Phase 2: [backend/app.py](../backend/app.py) — POST/GET `/api/parent-snapshot`, хранилище `backend/data/parent_snapshots.json`, TTL 7 дней; ProfileView — кнопка «Создать код для родителя» (при accessToken и роли participant/parent), модалка с кодом, QR (qrcode.react), «Скопировать ссылку»; обработка `?parent_code=` при загрузке и блок «Ввести код от ребёнка» в модалке. [.memory-bank/tech_context.md](../.memory-bank/tech_context.md) — контракт parent-snapshot API. |
| Done         | MVP «Смены и отряды» (staff flow) | [backend/app.py](../backend/app.py): `SHIFTS_FILE`, `_require_organizer_jwt()`, `_shifts_load()`/`_shifts_save()`, GET/POST `/api/shifts`, GET/POST `/api/shifts/<id>/squads`, POST `/api/organizer/generate-code`. [authRole.ts](../src/types/authRole.ts): `canCreateShiftsAndSquads(role)`. [ProfileView.tsx](../src/views/ProfileView.tsx): блок «Смены и отряды» для shift_leader/developer (список смен, отряды, создание смены/отряда, выдача кода по JWT). Хранение: `backend/data/shifts.json`. [docs/REPORT_STAFF_FLOW_SHIFTS_SQUADS_MVP.md](REPORT_STAFF_FLOW_SHIFTS_SQUADS_MVP.md). |
| Done         | UX-шлифовка блока «Смены и отряды» | [ProfileView.tsx](../src/views/ProfileView.tsx): `loadOrganizerData` (useCallback) с `fireOn401()` при 401; кнопка «Обновить»; fireOn401 во всех organizer API (GET shifts/squads, POST смены/отряда/generate-code); пустое состояние «Войдите через код верификации для управления сменами» при !accessToken; кнопки действий только при accessToken. |
| Done         | Обработка ошибок teams API и кнопка «Повторить» | [TeamContext.tsx](../src/context/TeamContext.tsx): `loadError` state, установка/сброс при загрузке mine и в `syncTeam`; при !res.ok (не 404) — `loadError: 'server'`, в catch — `'network'`; в `joinTeam` при 404 → return false, 409 → throw `already_in_another_team`, иначе при !res.ok → throw `server_or_network`. [TeamDashboard.tsx](../src/components/TeamDashboard.tsx): при !myTeam && loadError — блок «Не удалось загрузить Движок…» и кнопка «Повторить» (syncTeam); при ошибке join (сеть/5xx) — `joinRetryVisible`, сообщение и кнопка «Повторить» (повтор handleJoin). [src/types/teams.ts](../src/types/teams.ts): `loadError: string | null` в TeamContextType. |
| Done         | Консистентность пустых состояний и ошибок в блоках ЛК | [TeamContext.tsx](../src/context/TeamContext.tsx): fireOn401() при 401 во всех методах (load, syncTeam, createTeam, joinTeam, updateTeam, leaveTeam, deleteTeam). [TeamDashboard.tsx](../src/components/TeamDashboard.tsx): при loadError — блок с user-facing сообщением и «Повторить» (syncTeam). [CouncilDashboard.tsx](../src/components/CouncilDashboard.tsx): блок «Связь с Движками» при loadError — фиксированный текст «Проверь подключение к интернету», стиль блока, «Повторить». [SquadCornerDashboard.tsx](../src/components/SquadCornerDashboard.tsx): isEmptySquad, profile-empty-state «Данные отрядного уголка пока не заполнены». [ProfileView.tsx](../src/views/ProfileView.tsx): панель входящих заявок — eventsHasLoaded, три пустых состояния (секрет / загружено пусто / до загрузки), при eventsError — блок с «Повторить»; CounselorSquad — organizer-empty-state для входа по коду и создания отряда. |
| Done         | Герб Движка шаг 2 — UI под ИИ-генерацию | [TeamDashboard.tsx](../src/components/TeamDashboard.tsx): state gerbError, сброс при открытии/закрытии модалки «Создать визуал команды»; requestGerbImage(teamId, style, accessToken) — fetch POST /api/teams/gerb-generate, разбор imageUrl/imageBase64; handleGerbGenerate (try/catch/finally) — вызов requestGerbImage, при ошибке setGerbError и блок «Не удалось сгенерировать герб» + кнопка «Повторить»; при успехе — превью и «Сохранить как герб». [.memory-bank/tech_context.md](../.memory-bank/tech_context.md): контракт POST /api/teams/gerb-generate (планируемый). Бэкенд не реализуется в этой задаче. |
| Done         | Герб Движка шаг 2 (backend ИИ) | [backend/app.py](../backend/app.py): POST `/api/teams/gerb-generate`, слой провайдеров (image_providers.get_image_provider), валидация teamId/style, проверка членства. 200 — `{ imageBase64 }`; 503 — нет ключа или ошибка. [.memory-bank/tech_context.md](../.memory-bank/tech_context.md) — контракт. |
| Done         | Backend ИИ-изображения: универсальный API и провайдеры | [backend/image_providers.py](../backend/image_providers.py): generate/process, OpenAI + заглушки (fusion_brain, yandex_art, gigachat, alice). [backend/app.py](../backend/app.py): POST `/api/images/generate` (mode generate\|process, context, prompt?, imageBase64?), IMAGES_CONTEXT_PROMPTS, IMAGE_PROVIDER; gerb-generate переведён на провайдеры. Контракт в tech_context.md § POST /api/images/generate. |
| Done         | GET /api/health (liveness) | [backend/app.py](../backend/app.py): GET `/health` — проверка с DATA_FILE (503 при отсутствии); GET `/api/health` — liveness, всегда 200, `{"status":"ok"}`, без auth. [.memory-bank/tech_context.md](../.memory-bank/tech_context.md): контракт GET /api/health. |
| Done         | Синхронизация промптов backend ↔ cf-api (единый тон NeuroValyusha) | [scripts/sync-cf-api-prompts.mjs](../scripts/sync-cf-api-prompts.mjs): facts.json → generated_camp_facts.ts, Python export_system_prompt → generated_chat_prompt.ts. [cf-api/src/neurovalyusha/](../cf-api/src/neurovalyusha/): camp_facts.ts импортирует generated; constants.ts использует NEUROVALYUSHA_FULL_CHAT_PROMPT для chat-basic/chat-with-badge. [docs/DATA_SYNC.md](DATA_SYNC.md) § промпты, [cf-api/DEPLOY_PRODUCTION.md](../cf-api/DEPLOY_PRODUCTION.md), [cf-api/prompts/README.md](../cf-api/prompts/README.md). |
| Done         | ИИ-изображения во всех кабинетах (UI) | [src/utils/imageGenerateApi.ts](../src/utils/imageGenerateApi.ts): requestImageGenerate(mode, context, prompt?, imageBase64?), getApiBase, userMessageFromStatus. [SquadCornerDashboard.tsx](../src/components/SquadCornerDashboard.tsx): onGenerate/onProcess для 4 слотов (squad_corner). [WingDashboard.tsx](../src/components/WingDashboard.tsx): onGenerate/onProcess (wing). [ProfileView.tsx](../src/views/ProfileView.tsx): passport_avatar и workshop_badge — onGenerate/onProcess (passport, workshop). [TeamDashboard.tsx](../src/components/TeamDashboard.tsx): team_flag — onGenerate/onProcess. Герб остаётся на /api/teams/gerb-generate. [.memory-bank/tech_context.md](../.memory-bank/tech_context.md) § ИИ-изображения. |
| Done         | CI: self-check и job backend-health | [.github/workflows/ci.yml](../.github/workflows/ci.yml): в job lint-and-test шаг «Run self-check» после Install dependencies; отдельный job backend-health (Backend health check) — Node.js + Python, npm ci, pip install -r backend/requirements.txt, запуск backend в фоне, ожидание GET /api/health 200, BACKEND_URL=http://localhost:4000 npm run self-check. |
| Done         | UX при 429/503 для ИИ-изображений | [src/utils/imageGenerateApi.ts](../src/utils/imageGenerateApi.ts): в `userMessageFromStatus` при 503 — «Сервис генерации временно недоступен»; при 429 — «Слишком много запросов. Попробуйте позже.» и подсказка по retryAfter («Можно повторить через минуту» / «Повторите через N мин.»). Сообщения отображаются в модалках ImageSourceBlock (generate и process) с кнопкой «Повторить». Для герба Движка в [TeamDashboard.tsx](../src/components/TeamDashboard.tsx) в `requestGerbImage` при 503 — «Сервис генерации временно недоступен», при 429 — сообщение о лимите и при retryAfter 60 подсказка «Повторить через 1 мин.» (без английского текста с бэкенда). |
| Done         | Режим Process в ImageSourceBlock (аудит, a11y) | [ImageSourceBlock.tsx](../src/components/ImageSourceBlock.tsx): кнопка «Обработать ИИ» показывается при `onProcess`; aria-label «Обработать изображение с помощью ИИ». Аудит: onProcess передаётся во всех разделах с ИИ-изображениями (флаг Движка, отрядный уголок, Крыло, паспорт, мастерская); герб — только generate, без Process. |

---

## Completed (do not redo)

Ниже — плоский список всего с статусом **Done**. Не перереализовывать; при сомнении проверь Evidence.

- **Среда и доки:** SERVERS.md — секция Dev-сервер (Vite), порт по выбору (3001/3009 и т.д.).
- **Первый вход в ЛК:** ProfileView — 4 шага туториала (паспорт, В пути, Коллекция, Помощь ИИ); селекторы #profile-passport-card, #profile-tab-active, #profile-tab-collection, #profile-chat-trigger; HintOverlayContext вызывает onComplete в hideHint; meta.hasCompletedTutorial.
- **Резервная копия:** ProfileView — блок «Прогресс хранится на этом устройстве», кнопка «Сделать резервную копию», «Восстановить из файла».
- **Тестовый режим:** tech_context.md секция «Development: Test Mode», README раздел «Разработка» — rl_guide_test_mode только для разработки.
- **CODEX A/B/C:** закрыты пунктами среда, резервная копия, тест-режим, Share Center.
- **Share Center:** ProfileView — два формата 9:16/16:9, тумблер «Скрыть ник», пригласить друзей, экспорт/импорт, автоскролл к #share/#share-center.
- **Мастерская (A1–A3 и флоу):** разблокировка по 1.16.1/1.16.2, параметризация по categoryId из URL, триггеры в BadgeView/CategoryView, полный флоу после создания (confirm, Telegram, карточка).
- **Part B:** Мои предложения в Мастерской, Rank Up оверлей, npm run sync:ai-data + docs/DATA_SYNC.md.
- **Slice 2–4:** Social/Viral MVP (SocialGenerator, Share Preview), Smart Onboarding (HintOverlayContext, SmartHint, туториал), Creator Workshop (форма, 4К, Telegram).
- **Sprint 1 (Инкубатор):** загрузка community_badges из localStorage (кэш) + API; возврат communityBadges из useDataLoader; блок «Идеи Сообщества» во вкладке Мастерская в ProfileView.
- **Техдолг (community_badges):** бэкенд — валидация и санитизация POST, rate limit по IP; фронт — очередь публикаций в localStorage при offline, обработчик online, индикация Оффлайн/Синхронизация/В очереди.
- **Sprint 2 (прожектор на главной):** BlueNestLanding — всплывашка «О концепции игры» позиционируется от триггера через getBoundingClientRect(); conceptPopoverAnchor, updateConceptPopoverPosition; портал с top/left и translate(-50%, 0); обновление при resize.
- **Sprint 3 (социальный мост):** getBadgeShareUrl в socialGenerator; кнопки «Ссылка на значок» в BadgeView (тост) и BadgeLevelView (блок шаринга); открытие значка по URL ?view=badge&badgeId=... (useAppController). Карточки достижений с ИИ — уже в BadgeLevelView.
- **Качество шеринга (вирусный контент):** AI-слоган на карточке (customCaption), CTA и хештеги в футере, челлендж-фразы по seed, усиленные промпты в aiService с вайбом и контекстом; buildCaption с призывом и хештегами.
- **Карточка прогресса AAA:** progress_summary рендерится по [AAA_PROGRESS_CARD_SPEC.md](docs/AAA_PROGRESS_CARD_SPEC.md) §9: отдельный pipeline в socialGenerator (anchor-driven ProgressAnchors, порядок BG→FRAME→BRAND+PILL→AVATAR→RANK→STATS→SLOTS→BUFF→FOOTER; fitFontSize, два чипа статов), футер без артефактов.
- **Подробная анкета при подтверждении:** proofForm в ProfileView — секции с лейблами (Опыт, Реальный вклад, Ссылка, Фото), методическая подсказка, мягкая валидация, тост «Отправлено», закрытие по overlay.
- **Совет Лагеря (MVP):** CouncilDashboard — точка входа, описание механики, ссылка на значок 8.6.
- **Совет Лагеря (развитие):** обзор инициатив и протоколов (цикл Идеи→Решения→Задачи→Артефакты, типичные инициативы), связь с Движками (useTeam, скролл к team-dashboard).
- **Аналитика 4К:** profile4k.ts (маппинг категорий → 4К и Программа 2026), Profile4KDashboard — полоски по навыкам и по направлениям программы.
- **Реальный Отряд:** RealDiaryDashboard — Реальный Дневник (заголовок), презентация итогов с данными отряда (название, девиз), связь с Инспектором Пользы (скролл к миссиям, подсказка всегда видна), тост «Скопировано» вместо alert.
- **Наполнение Архитектора Отряда:** SquadArchitect — 16 традиций, duration/materials/counselorTip, копирование сценария, сохранение в userProgress, diarySquadName из Реального Дневника.
- **Этап 5 (синхронизация с чатом):** ЛК в current_view, ответы бота на экране ЛК проверены.
- **ЛК: Предложить инициативу в совет лагеря:** fetchCouncilInitiative в aiService; кнопка в CouncilDashboard, TeamDashboard, WingDashboard; модалка в ProfileView (форма, генерация, копировать/Telegram).
- **ЛК: Создать движок (устойчивость):** TeamContext — try/catch и alert при QuotaExceededError; slimTeamForStorage; защита userData.profile в createTeam/handleCreate.
- **ЛК: Штаб Крыла видимость:** WingDashboard всегда рендерится; при отсутствии wingId — входное состояние с «Сформировать Крыло»; при wingId — полный блок с аватаром Крыла и «Добавить фото».
- **Значки на флаг отряда (категория 10):** flagBadgeRequests/flagBadgesApproved в squad; ProgressContext: addFlagBadgeRequest, approveFlagBadgeRequest, rejectFlagBadgeRequest; SquadCornerDashboard — секция «Значки на флаг отряда» (на флаге, заявки); кнопка «Отправить все заявки на рассмотрение» (Telegram с объединённым текстом); пояснения в пустом состоянии; BadgeView при «В путь» для 10.x и BadgeLevelView при достижении 10.x.1 создают заявку.
- **ЛК: загрузка фото (Движок, Отрядный уголок):** TeamDashboard — загрузка фото флага отряда (teamPhoto); SquadCornerDashboard — 4 фото (уголок, флаг, отряд, с вожатыми). Штаб Крыла — см. «ЛК: Штаб Крыла».
- **План получения значка: качественная генерация и UX «Дополнить»:** levelId при setPlanFormBadge; BadgePlanInput с nameExplanation/skillTips/confirmation; промпт с блоками значка; кнопка «Дополнить» на result, «Изменить контекст»; подсказки при пустом контексте; max_tokens 2400 в chatbot; логирование camp-program.
- **Лента с лайками (Идеи Сообщества):** useDataLoader — communityLikedIds, toggleCommunityLike, localStorage; ProfileView — кнопка лайка (Heart) у каждой идеи.
- **Формат событий и Webhook (Telegram):** backend/events.py — ConfirmationRequestedEvent, LevelAchievedEvent, append_confirmation_event, get_confirmation_events; backend/app.py — webhook endpoint, send_telegram_message; scripts/set_telegram_webhook.py; docs/EVENTS_AND_WEBHOOKS.md.
- **Ответ бота при приёме заявки:** send_telegram_to_chat в backend/app.py; вызов после append_confirmation_event с текстом «Заявка принята, вожатый посмотрит».
- **Роли, авторизация, адаптация ЛК:** authRole (canSeeOtradBlocks, showEventsPanelForRole); ProfileView — скрытие отрядных блоков для traveler, родитель как участник (кнопки не удаляются), панель заявок для counselor/shift_leader; ChatBot — лимит с бэкенда (GET /api/chat/limits); backend CHAT_MESSAGES_PER_DAY, GET /api/chat/limits.
- **Кабина космического корабля (ЛК):** ProfileView — cabin layout с панелями (Инспектор, Паспорт, Отряд вожатых, Отрядный уголок, Реальный Дневник, Движок, Совет, БРО, Мастерская, Шеринг, Вожатификатор, 4К, Для родителей), нижняя консоль; profile-view-spaceship.css, main.tsx (profile-spaceship-root); сервер кабины — порт 3010, profile-desktop.html.
- **Унификация личного кабинета:** Одна точка входа main.tsx (обёртка profile-spaceship-root, импорт profile-view-spaceship.css); profile-desktop.html → main.tsx, main-profile-desktop.tsx удалён; Vite multi-page (index.html + profile-desktop.html); ЛК с кабиной единообразен на портах 3001, 3010, 3002. Доки PROFILE_CABIN_SERVER_SETUP.md, SERVERS.md обновлены.
- **Серверный RBAC для чата:** backend/app.py — CHAT_ALLOWED_ROLES, _require_chat_auth() (Authorization: Bearer, JWT, роль); вызов в chat_with_bot(); 401/403. ChatBot.tsx — всегда Authorization при accessToken; обработка 401 (clearAuth) и 403. tech_context.md — контракт POST /api/chat.
- **Shift_leader и отряд из вожатых (MVP):** authRole — shift_leader, canCreateCounselorSquad; CounselorSquadContext — createSquad, joinByCode, inviteCode/link, ?counselor_squad; ProfileView — панель counselor-squad (создание отряда для shift_leader/developer, вход по коду для counselor). Backend: CHAT_ALLOWED_ROLES с shift_leader, verify-code выдаёт JWT. SANDBOX_TESTING.md §6.
- **Обмен итогами Реального Дневника:** RealDiaryDashboard — кнопка «Отправить в Telegram» рядом с «Копировать»; handleSendToTelegram (buildPresentationText → t.me/Stivanovv?text=...); тост; fallback при ошибке; кнопка отключена при пустых данных. Паттерн как в proofForm и модалке инициатив.
- **ИИ-Манифест Маршрута:** BadgeView — тост после «В путь», «Поделиться» генерирует карточку 9:16 с манифестом (навык из profile4k, вызов от ИИ route_manifest_challenge); socialGenerator — manifestSkill, sub и customCaption в формате манифеста; fallback при отсутствии ИИ.
- **Учёт лимита сообщений чата на бэкенде:** backend/app.py — _check_and_inc_chat_daily, chat_daily_usage.json (по deviceId, UTC), вызов в chat_with_bot(); при превышении — 429. ChatBot — обработка 429 без clearAuth(). tech_context — 429 для POST /api/chat.
- **Связность Дневник ↔ Инспектор:** RealDiaryDashboard — getDiaryInspectorHint (подсказки по миссии дня и записям); InspectorDashboard — onOpenDiary, карточка «Открыть Дневник»; ProfileView — setActiveTab в onScrollToInspector, onOpenDiary в оба InspectorDashboard.
- **Slice 4: Мой арт (локальные скины):** userProgress — customBadgeImages; ProgressContext — setCustomBadgeImage, миграция; BadgeIcon — baseBadgeId и custom/legacy; BadgeView — переключатель «Мой арт» / «Загрузить свой арт» / «Удалить арт», hero по custom; BadgeLevelView — hero по custom; ProfileView — загрузка изображения в Кузнице, setCustomBadgeImage при сабмите; tech_context — секция «Мой арт».
- **Родитель по коду:** Phase 1 — отчёт для родителя (файл/ссылка), модалка с файлом и ссылкой, `?parent_view=` при загрузке. Phase 2 — POST/GET /api/parent-snapshot, код 6–8 символов, TTL 7 дней, кнопка «Создать код для родителя», модалка с кодом/QR, «Ввести код» в модалке и `?parent_code=` при загрузке.
- **MVP «Смены и отряды» (staff flow):** backend — shifts.json, _require_organizer_jwt, GET/POST /api/shifts, /api/shifts/:id/squads, POST /api/organizer/generate-code; authRole — canCreateShiftsAndSquads; ProfileView — блок «Смены и отряды» (shift_leader/developer), модалки смена/отряд/код. Отчёт: docs/REPORT_STAFF_FLOW_SHIFTS_SQUADS_MVP.md.
- **UX-шлифовка блока «Смены и отряды»:** loadOrganizerData с fireOn401, кнопка «Обновить», пустые состояния при !accessToken, кнопки только при accessToken.
- **Консистентность пустых состояний и ошибок в блоках ЛК:** TeamContext — fireOn401 при 401; TeamDashboard/CouncilDashboard — loadError, блок с «Повторить»; SquadCornerDashboard — isEmptySquad, profile-empty-state; ProfileView — eventsHasLoaded, три пустых состояния заявок, eventsError с «Повторить»; CounselorSquad — organizer-empty-state.
- **Онлайн-Движки (расширение): предпросмотр по коду:** TeamDashboard — при вводе кода debounced GET /api/teams/:id, превью (название, девиз) или «Код не найден»; при загрузке с ?engine= — декод приглашения, подстановка кода и превью, раскрытие блока «Войти по коду».
- **Герб Движка шаг 2 (backend ИИ):** backend — POST /api/teams/gerb-generate, _generate_gerb_image через OpenAI GPT Image 1.5, промпт из team name/nickname/style; TeamDashboard — передача gerbPrompt; tech_context — контракт.
- **Режим Process в ImageSourceBlock (аудит, a11y):** ImageSourceBlock — aria-label для кнопки «Обработать ИИ»; аудит подтвердил передачу onProcess во всех разделах (флаг, отрядный уголок, Крыло, паспорт, мастерская), герб без Process.

---

*При завершении новой задачи: обнови статус в таблице на Done, добавь Evidence, сдвинь «Где мы сейчас». Детали — в [.memory-bank/progress.md](../.memory-bank/progress.md).*
